# Copier ce fichier en `.env` (jamais commité — déjà listé dans .gitignore)
# et renseigner les valeurs ci-dessous.
#
# Projet Supabase utilisé : "apkzmb-cloud's Project" (id: acqxiwedxproqjffnerb).
# Ce projet est PARTAGÉ avec d'autres apps — le schéma Garam Paparas vit
# entièrement dans des tables préfixées `kora_*`, isolées du reste (voir
# la migration kora_schema_and_auth_foundations). Aucune autre table n'a
# été touchée.

# URL du projet Supabase (Project Settings > API > Project URL)
VITE_SUPABASE_URL=https://acqxiwedxproqjffnerb.supabase.co

# Clé publique anon (Project Settings > API > anon/public key).
# Cette clé est sûre à exposer côté client : c'est tout le principe de
# Row Level Security (RLS) — voir les policies `kora_*` dans la migration.
# NE JAMAIS mettre la service_role key ici (elle contourne RLS).
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFjcXhpd2VkeHByb3FqZmZuZXJiIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODEwMDIzNjEsImV4cCI6MjA5NjU3ODM2MX0.ffRgU5K7m2tTRxGb6uDejZVhpAYpJ1QOJlOXhzpfaVc
